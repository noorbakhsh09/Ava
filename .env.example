# Runtime secrets are configured in the Electron UI and stored with OS-backed encryption.
# DATABASE_URL is used only by Prisma CLI commands such as `bun run db:migrate`.
DATABASE_URL=postgresql://ava:change-me@127.0.0.1:5432/ava

# Optional development overrides.
AVA_SETTINGS_PATH=
LOCAL_API_PORT=3421
